services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: admission_coach
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d admission_coach"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
